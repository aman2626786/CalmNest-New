{
  "name": "calmnest-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd CalmNest && npm install && npm run build",
    "start": "cd CalmNest && npm start",
    "dev": "cd CalmNest && npm run dev"
  },
  "workspaces": [
    "CalmNest"
  ]
}